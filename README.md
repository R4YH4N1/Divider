# The file with the code for the divider is called "Program.cs"

Also, for those who actually want to know what it does:

First number input / Second Number input = Answer "r" Modulus

                                             (^r=remainder^)
